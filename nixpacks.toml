[phases.setup]
nixPkgs = ["ffmpeg", "libsodium", "libffi"]

[phases.build]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "uvicorn dashboard:app --host 0.0.0.0 --port $PORT"
